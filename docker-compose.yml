services:
  bot:
    image: ghcr.io/miteshn/discord-music-bot:latest
    restart: always
    volumes:
      - ./cache:/app/cache
    environment:
      - DISCORD_BOT_TOKEN=
      - SPOTIFY_CLIENT_ID=
      - SPOTIFY_CLIENT_SECRET=
      - GENIUS_API_TOKEN=
      # - CACHE_LIMIT_MB=2048
      # - MAX_CACHE_DURATION=1800
